<%= form_for(admin_career) do |f| %>
  <% if admin_career.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_career.errors.count, "error") %> prohibited this admin_career from being saved:</h2>

      <ul>
      <% admin_career.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
